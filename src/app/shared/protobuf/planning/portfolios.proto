syntax = "proto3";

option go_package = "planning/portfolios";
import "google/api/annotations.proto";

service Portfolios {
    rpc Create (Request) returns (Response) {
      option (google.api.http) = {
        post: "/user/v1/portfolios"
      };
    } 
    
    rpc Update (Request) returns (Response) {
      option (google.api.http) = {
        put: "/user/v1/portfolios"
      };
    }                      
  
    rpc Read (Request) returns (Response) {
      option (google.api.http) = {
        get: "/user/v1/portfolios"
      };
    }

    rpc Delete (Request) returns (Response) {
        option (google.api.http) = {
          delete: "/user/v1/portfolios"
        };
      }
  }

  // SignUp.
message Request {
    string name = 1;
}

message Response {
    string name = 1;
}